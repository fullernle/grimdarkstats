import {factionColors} from "./index";
import regeneratorRuntime from "regenerator-runtime";
import * as d3 from "d3";

export const drawFaction = async (factionData) => {
	console.log(factionData);

	
}